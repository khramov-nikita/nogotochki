# Cloud — агентный бриф

Документ для ИИ-агентов, которые продолжают фулстек «Ноготочки». Продуктовый UI — HTML/CSS/JS в `web/` (MPA, без React и без продуктового бандлера). Vite в `web/` только локально: раздаёт страницы и проксирует `/api` на `:3000`. Живая SQLite — папка `server/` в этом репозитории, прод бэкенда — **BeGet**, не serverless. Не подключай Lovable, Stripe, корзину. Схема в `docs/db-schema.md`, реализация в `docs/database.md` — не изобретай другую и не заводи таблицу свободных слотов. Колонку `clients.role` не добавлять: роли — список в `client_roles` (человек может быть мастером и администратором сразу). Админ не через `ADMIN_EMAILS`.

Правила верстки: `docs/frontend-rules.md`. Карта экранов ↔ API: `docs/ui-map.md`. Ручной чек-лист и находки: `docs/test-checklist.md`. Папку `server/` не трогай, пока человек не попросил (админка услуг/мастеров/записей уже с серверной логикой; миграции `005`–`008`). HTTP только через `web/js/api.js`. Стили — только `var(--…)` из `web/tokens.css`. Узкий экран — только `@media` в `web/responsive.css`, десктопные селекторы не переписывать. Vite в dev проксирует на API **только HTML** `/admin`, `/admin/services`, `/admin/masters` — не `/admin.css`.

Проект в v0 не создавай и не публикуй. Макеты смотри в Figma, код пиши сюда. HTTP API — `server/src/http/` и `server/src/domain/`; файл БД сам по себе не сервис.

## Что это за продукт

Веб-сервис записи бьюти-студии «Ноготочки»: клиент сам выбирает услуги и мастера, бронирует слот, управляет визитом в кабинете.

Полный паспорт: `Паспорт сервиса «Ноготочки».md`  
Путь клиента (согласован): `Путь клиента.md`  
Схема БД (согласована): `docs/db-schema.md`  
Реализация БД / журнал (миграции, драйвер, команды, BeGet): `docs/database.md`  
Правила верстки: `docs/frontend-rules.md`  
Карта экранов: `docs/ui-map.md`  
Ручная проверка (результаты и приоритет находок): `docs/test-checklist.md`  
Репозиторий: https://github.com/khramov-nikita/nogotochki (private, ветка `main`)  
Визуал и компоненты: `figma.md` и https://www.figma.com/design/PN8kfs5KAmLwwqZIKmnAOG (`PN8kfs5KAmLwwqZIKmnAOG`). Библиотека Figma добрана. **Готово на `Screens`:** лендинг `Landing — Ноготочки` (`node-id=45-3`, 1440), каталог услуг `Catalog — Услуги` (`node-id=80-409`, 1440), каталог мастеров `Catalog — Мастера` (`node-id=88-270`, 1440), карточка мастера `Master — Карточка` (`node-id=104-319`, 1440), авторизация: `Auth — Вход` (`110:374`), `Auth — Регистрация` (`111:428`), `Auth — Письмо отправлено` (`111:792`), `Auth — Новый пароль` (`111:833`), `Auth — Пароль изменён` (`111:919`), `Auth — Вход / ошибка` (`111:958`), запись шаг 1: `Booking — 1 Услуги` (`127:636`), `Booking — 1 Только дизайн` (`128:699`), шаг 2: `Booking — 2 Мастер` (`132:769`), `Booking — 2 Нет мастера` (`132:817`), шаг 3: `Booking — 3 Дата и время` (`141:1324`), `Booking — 3 Нет слотов` (`141:1821`), шаг 4: `Booking — 4 Проверка / гость` (`147:1438`), `Booking — 4 Проверка / вход выполнен` (`147:2468`), шаг 5: `Booking — 5 Готово` (`147:2588`), `Booking — Резерв истёк` (`147:2733`), `Booking — Слот занят` (`147:2825`), кабинет: `Cabinet — Активные` (`151:1561`), `Cabinet — Пусто` (`151:1667`), `Cabinet — История` (`151:1982`), `Cabinet — Детали / ожидает предоплату` (`152:1770`), `Cabinet — Детали / подтверждена` (`152:2128`), `Cabinet — Отмена` (`153:2034`), `Cabinet — Перенос` (`154:1904`), `Cabinet — Перенос / нет слотов` (`155:2049`), `Cabinet — Профиль` (`157:2192`), `Cabinet — Уведомления` (`157:2244`), `Cabinet — Правила отмены и переноса` (`157:2296`), служебные: `Error — 404` (`161:2351`), `Error — Сервер` (`161:2388`), `Error — Сессия истекла` (`161:2408`), политика: `Legal — Политика конфиденциальности` (`163:2510`). На auth-фреймах ссылка «Войти» в шапке скрыта; на шагах 1–3 записи, проверке гостя, служебных и политике — видна; на «вход выполнен», «Готово», «Слот занят» и кабинете — LoggedIn («Кабинет»). Профиль, уведомления, правила, служебные и политика уже в макете.

В репозитории — паспорт, брифы, путь клиента, схема, каркас SQLite (`server/`) и продуктовые страницы `web/*.html`. Промпты экранов — в `prompts/` (в `.gitignore`, в git не коммитить). Черновой hash-UI (`web/src/`, `web/test.html`) — не продукт, не расширяй его. Файл `data/nogotochki.sqlite` и `.env` в git не входят.

## Что уже собрано в `web/`

Не переписывай эти экраны «с нуля» и не дублируй их в React/v0.

| Срез | Страницы | Состояние |
|---|---|---|
| Лендинг | `web/index.html` | Живой: услуги и мастера с API, CTA в запись |
| Авторизация | `web/auth.html`, `web/register.html` | Живой login/register + cookie-сессия; Ghost «Войти через Яндекс» → `POST /api/auth/yandex`. Reset-HTTP нет: «Забыли пароль?» только через `POST /api/auth/password-login-available` (аккаунт без пароля → Alert про Яндекс; иначе статичный HTML «Письмо отправлено») |
| Запись 1–5 | `booking.html` → `booking-master.html` → `booking-datetime.html` → `booking-review.html` → `booking-done.html` | Живой степпер, hold + таймер на шаге 3, гость/сессия на проверке, 409 `SLOT_TAKEN` на той же странице, истекший резерв |
| Кабинет записей | `cabinet.html`, `cabinet-appointment.html`, `cabinet-reschedule.html`, `cabinet-rules.html`, `cabinet-notifications.html` | Живые активные/история/empty, детали, модалка отмены, перенос без hold, правила как контент, лента уведомлений (`GET /api/notifications`). Профиль **не верстали** |
| Админка | `GET /admin`, `/admin/services`, `/admin/masters` (HTML из Express по роли `administrator`; UI — `web/js/admin-appointments.js`, `admin-services.js`, `admin-masters.js`, `admin-common.js`) | Живые: записи дня (фильтр мастера, confirm предоплаты, отмена/перенос чужих, блокировки времени, создание поверх с `overlap_override`); услуги и мастера (список вкл. отключённых, форма, вкл/выкл, удаление). Отдельных кадров админки в Figma нет. Стили — `web/admin.css` |
| Общее | `web/js/api.js`, `header.js`, `store.js`, `format.js`, `visit-summary.js`, `require-client.js`, `tokens.css`, `responsive.css`, `admin.css` | Все HTTP через `api.js`. Черновик записи — `sessionStorage` (`store.js`). Гость на кабинете: `GET /api/auth/me` 401 → `auth.html?next=`. Узкий экран (`max-width: 47.99rem`): меню-оверлей, степпер без наезда, календарь сверху / слоты в 2 колонки, `.booking-dock` внизу, кабинет карточками, модалка отмены по высоте. На телефоне то же, что на компьютере; десктоп не трогать |

Локальный запуск: `npm start` в `server/` (порт 3000) и `npm run dev` в `web/` (порт 5173). Не открывай HTML двойным щелчком. Vite проксирует на API только HTML `/admin`, `/admin/services`, `/admin/masters` — не `/admin.css` и не прочие `/admin*`.

## Стек

- **Продуктовый UI:** обычные страницы в `web/`, не Next.js и не React. Vite — только dev-сервер, прокси `/api` и HTML-страниц `/admin` (не CSS).
- **Бэкенд / прод файла БД:** BeGet, один процесс Node. Не Vercel serverless: SQLite там не живёт.
- **Черновик записи:** `sessionStorage` через `web/js/store.js` (услуги, мастер, дата, `hold_token`). Не React context.
- **Auth:** живые сессии из `sessions` (в БД SHA-256 токена, срок `SESSION_DAYS`), пароль — salted scrypt в `password_hash` (может быть NULL у входа через Яндекс). Внешний вход: `provider` / `provider_id` на `clients`; `POST /api/auth/yandex` → `fetchYandexUserProfile` в `server/src/domain/yandex-oauth.js` (обмен code → token → профиль; токен Яндекса не хранить). Env: `YANDEX_CLIENT_ID`, `YANDEX_CLIENT_SECRET`, `YANDEX_REDIRECT_URI`. Старт: `GET /api/auth/yandex/start`; возврат: `/auth/yandex/callback`. Роли — список в `client_roles`; новым через OAuth только `client`. Login/register/yandex с rate limit по `req.ip`; `X-Forwarded-For` только при `TRUST_PROXY` (на BeGet за nginx — `1`, иначе пусто). В теле login/register/yandex можно передать `hold_token`.
- **БД:** уже в репозитории. SQLite файл `data/nogotochki.sqlite`, драйвер встроенный `node:sqlite` (`DatabaseSync`), Node **≥ 22.16** (`server/package.json` `engines`). Открытие только через `server/src/db/connection.js`, `PRAGMA foreign_keys = ON`. Схема — 19 таблиц из `docs/db-schema.md` (плюс `roles` и `client_roles`). Миграции: `005` — `services.is_active` + снимок цены/длительности в `appointment_services`; `006` — админ-записи / журнал отмены-переноса / `master_time_blocks.kind`; `007` — тело уведомлений; `008` — OAuth (`password_hash` nullable, `provider` / `provider_id`). Таблицы нарезанных слотов нет: окна считаются из графика, записей, блоков и холдов. HTTP API — `server/src/http/` (`npm start` из `server/`). Секреты только в `.env` (в git — `.env.example`); локальный тестовый админ — только `.env.test` (в git не входит). Настройки салона — в `studio_settings`, не в env.
- **Команды** (из `server/`): `npm run db:reset` — удалить локальный файл и накатить миграции; `npm run migrate`; `npm run seed` / `seed:dev`. `db:reset` и `seed:dev` не для production.
- **Оплата:** нет. Stripe, checkout, корзина, промокоды запрещены.
- **UI:** не дефолтный цветной shadcn. Токены — `web/tokens.css`, экраны — `figma.md`.

## Как агенту работать

Сейчас:

1. Новых фреймов на `Screens` не добавляй, пока человек не попросит. Регистрация и вход начинаются с лендинга («Войти» в шапке публичных страниц), не со степпера; на auth-страницах **не возвращай «Войти» в шапку**. На шагах 1–3 записи, проверке гостя, служебных и политике «Войти» в шапке оставь. На кабинете — LoggedIn.
2. Не собирай фулстек заново. Не пересоздавай схему, не ставь `better-sqlite3`. Не `profiles`, не `slots`, не колонку `clients.role`. Пароль — `crypto.scrypt`. Не возвращай `ADMIN_EMAILS`.
3. Следующий срез UI или багфикс — только по просьбе. Empty того же шага — состояние той же HTML-страницы, не новый файл. Админку услуг/мастеров/записей и вход через Яндекс не собирай заново.
4. `server/` не меняй «заодно с версткой». Нет эндпоинта — скажи человеку.
5. Ошибку API показывай текстом на экране (`error.message`). Свой дедлайн «за 24 часа» не пиши: в сиде `cancel_min_hours_before` / `reschedule_min_hours_before` = `NULL`. «Мало времени» = `can_cancel === false` / `can_reschedule === false` или 409 `CONFLICT`.
6. Ручная проверка 2026-09-10 зафиксирована в `docs/test-checklist.md` (результаты + группы находок). **Не чини находки сам**, пока человек не попросил. Когда попросят чинить — бери порядок из раздела «Находки» в чек-листе (сначала G4/дизайн в записи, затем C17 empty на переносе, затем текст S3, затем G3 сертификат; G3+G4 могут быть одной причиной в каталоге/сиде).

Дальше:

1. Одно требование за раз.
2. Спорный подход согласуй, потом кодь.
3. «Агент написал, что сделал» ≠ сделано: проверяй в браузере на http://localhost:5173/ .
4. Перед новым экраном открой строку в `docs/ui-map.md`.

## Данные, которые можно закладывать в продукт

Услуги (не выдумывать другие цены):

| Услуга | Цена | Длительность |
|---|---|---|
| Маникюр с покрытием гель-лаком | 1 800 ₽ | 1,5 часа |
| Маникюр и педикюр | 3 200 ₽ | 2,5 часа |
| Наращивание ногтей | 2 800 ₽ | 2,5 часа |
| Дизайн ногтей | 300 ₽ за два ногтя | +15–30 мин к сеансу |
| Коррекция и окрашивание бровей | 1 200 ₽ | 40 минут |
| Ламинирование бровей | 1 800 ₽ | 1 час |
| Подарочный сертификат | 3 000 ₽ или 5 000 ₽ | 6 месяцев |

Студия: маникюр и брови, центр города, с 2019, несколько мастеров, только по записи.

## Обязательные продуктовые инварианты

- Сайт **не принимает оплату**. Слот резервируется на сайте, предоплату берёт студия вне сервиса.
- Статусы записи: ожидает предоплату / подтверждена / отменена / перенесена / истекла. Нет корзины, Stripe, checkout, промокодов.
- Слот держится ограниченное время. Нужен таймер резерва и сценарии «резерв истёк» / «слот уже занят» на тех же страницах степпера, не отдельный HTML «с начала».
- Точный адрес — только в кабинете после подтверждения (`status === "confirmed"` и непустой `exact_address`). Публично: «центр города».
- Имя мастера и контакты могут быть пустыми. Не генерировать фейковые ФИО и телефоны. Заголовок: «Мастер студии».
- Нет выезда на дом, нет карты клиента. Админ-панель уже есть: услуги, мастера и записи дня (`/admin`, `/admin/services`, `/admin/masters`). Кабинета сотрудника / «админки мастера» в v1 нет.
- Запись требует авторизации, чтобы **подтвердить**. **Старт входа и регистрации — лендинг**, не отдельный «дом» логина. На экранах auth в шапке **нет** повторного «Войти». Гость может выбрать услуги, мастера и слот. Черновик и резерв сохраняются (`Путь клиента.md`).
- Дизайн ногтей — добавка к сеансу, не отдельный визит. Подарочный сертификат в прайсе, но **не слот**: не класть в степпер. Отключённая услуга (`is_active = 0`) клиенту в записи не показывается; визит, оформленный раньше, остаётся со снимком цены и длительности.
- Нестандартные состояния обязательны: пустой кабинет, нет слотов, истёк резерв, слот занят, ошибки входа, потеря сессии, 404, сбой сервера. Ошибка входа — `Alert` в форме, не полноэкранный empty. Маппинг — в `Путь клиента.md` и `docs/ui-map.md`.

## Экраны

Не начинай с «ещё фич». Кабинет сотрудника не верстаем, пока человек не попросил. Админку (услуги, мастера, записи) не собирай заново — не дублируй hash-UI `web/src/pages/admin.js`.

**Уже в коде (не собирать повторно):**

1. Лендинг — `index.html`
2. Авторизация: вход, регистрация, «Войти через Яндекс» (OAuth; reset-HTTP не подключать)
3. Запись: услуги → мастер → дата/время → проверка → успех
4. Кабинет записей: активные, пустой список, история, детали, отмена, перенос, правила, лента уведомлений (`cabinet-notifications.html`)
5. Админка: записи (`/admin`), услуги и мастера (`/admin/services`, `/admin/masters`). Гость → вход, клиент без роли `administrator` → 403 HTML

**Открытые находки после ручной проверки** (детали и приоритет — `docs/test-checklist.md` §«Находки»): нет «Дизайн ногтей» на шаге 1 (G4); нет empty «Нет свободного времени» на переносе (C17); при конфликте слота на confirm текст «Резерв истек…» вместо «Это время уже занято» (S3); нет сертификата / «Условия в студии» на лендинге (G3). Чинить только по просьбе.

**Ещё нет отдельных HTML (следующий срез по просьбе):**

1. Каталог услуг `catalog.html`, каталог мастеров, карточка мастера — ссылки уже стоят, страниц нет
2. Политика `privacy.html`
3. Служебные: 404, сбой сервера, сессия истекла
4. Кабинет: профиль — в API нет смены пароля; не имитировать. Если попросят профиль — почта из `GET /api/auth/me`, поля пароля убрать (у OAuth-only пароля нет)

Авторизация и свободное время — с живой базой по `docs/db-schema.md`, не таблицей слотов. Горизонт календаря **8 недель**, резерв стартует **в момент выбора времени** на шаге 3. На переносе hold и Timer нет; слоты — `getAvailability` с `exclude_appointment_id`; текущий `starts_at` — отдельное состояние слота, не новый выбор.

Логика шагов и empty/error — `Путь клиента.md`. Последовательные промпты макетов — `prompts/Промпты прототипа.md` (один этап за вставку), но код уже пишется в `web/`, не в v0.

## Визуал в коде

Не ставь дефолтный цветной SaaS-шаблон. Токены — `web/tokens.css` (`var(--color-bg-canvas)` и аналоги), ориентир — `figma.md`:

- фон `#FFFFFF` / крем `#F5F3F0`
- акцент-блоки `#F4D7DA` и `#FEE5D8` только под фото, не на кнопках
- Primary — чёрный прямоугольник; Ghost — 1px чёрная обводка (это secondary); Text — без рамки; Danger — `var(--color-text-danger)` / inverse text, только «Отменить запись»
- заголовки — Playfair Display, UI — Inter
- radius 0 / 2px, пилюли запрещены
- русский UI, тон на «вы»
- шапка светлая минимальная (`web/js/header.js`), футер чёрный; корзины нет. Публично справа «Войти»; **на страницах входа/регистрации ссылки «Войти» в шапке нет**. На служебных и политике — Guest с «Войти».
- пароль — поле с глазом (`Field/Password`)
- карточка мастера: линия «ближайшее окно» или ряд слотов; нет имени → «Мастер студии»
- степпер записи: 5 шагов
- колонка записи и кабинета — 720 (`calc(var(--spacing-80) * 9)`)
- мобильное меню уже в `header.js`: оверлей Услуги / Мастера / Запись / Кабинет; логотип и «Войти» остаются в баре. Узкие правки — `web/responsive.css`, не дублировать в `landing.css` / `booking.css`

Спесимены Auth и Slot errors на странице Components в Figma — образец сборки форм и баннеров, не готовые экраны.

Лендинг верстай по фрейму `Landing — Ноготочки` (`45:3`), каталог услуг — по `Catalog — Услуги` (`80:409`), каталог мастеров — по `Catalog — Мастера` (`88:270`). Ориентиры лендинга уже в `index.html`:

- светлая шапка: логотип «Ноготочки», «Услуги / Мастера / Записаться», «Войти» или кабинет
- hero: «Запись в студию без звонков и переписки»; Primary «Записаться» + Ghost «Все услуги»
- полоса доверия только из паспорта
- «Как записаться»: 5 шагов; слот на ограниченное время; предоплату берёт студия
- семь услуг с ценами из таблицы, без скидок
- мастера: «Мастер студии», без выдуманных ФИО
- FAQ без аккордеона
- чёрный футер, в контактах только «центр города»

Каталог услуг, когда появится: сертификат с CTA «Условия в студии», не «Записаться» (на лендинге сертификата сейчас нет — находка G3). Каталог мастеров: три «Мастер студии» без ФИО, портрет 80×80. Карточка мастера — `Master — Карточка` (`104:319`). Авторизация — `Auth — Вход` (`110:374`): шапка Guest **без** «Войти», ошибка — `Alert` в форме; кнопка «Войти через Яндекс» в коде есть (в макете Figma её не было — оформить Ghost из токенов, не новый стиль). Запись — шаги 1–5 уже сверстаны; «Дизайн ногтей» на шаге 1 в проверке отсутствовал (G4). Кабинет записей — по `Cabinet — Активные` и соседним кадрам; отмена — оверлей на деталях; перенос — календарь без Timer, без ссылки «Другой мастер» в выбор мастера; лента уведомлений уже в `cabinet-notifications.html`. Профиль не верстать без просьбы. Служебные и политика — по `Error — *` и `Legal — Политика`. Поведение — `Путь клиента.md`: сертификат не в степпере, вход до подтверждения, адрес только после «подтверждена».

Кнопки: Large 24×16, Medium 16, Small 16×12. В коде не повторяй нулевой паддинг атома из библиотеки.

## Модель данных

Источник правды схемы: `docs/db-schema.md`. Как это собрано в коде: `docs/database.md`. Старый ориентир `profiles` / таблица `slots` со статусами free / reserved / booked **отменён**.

Каркас БД уже в `server/`. Когда человек попросит API или правки схемы:

- SQLite через `node:sqlite`, не `better-sqlite3`. `PRAGMA foreign_keys = ON` на каждом соединении. Клиент — `clients`, не `profiles` / `users`. Пароль — `password_hash` (salted `crypto.scrypt`, PHC с N/r/p и солью) или NULL при входе только через внешнего провайдера; не bcrypt и не argon2. Открытого пароля нет. Поля `provider` / `provider_id` для внешнего входа; токен провайдера не хранить. В `sessions` и `password_reset_tokens` — хеш токена, не сам токен. Роли — список в `client_roles`, не колонка `clients.role` и не `ADMIN_EMAILS`. SQL только параметризованный. Ошибки клиенту без текста SQLite и путей. Node на BeGet: **22 LTS ≥ 22.16 или 24**.
- Таблицы: `studio_settings`, `studio_closures`, `content_pages`, `content_page_items`, `clients`, `sessions`, `password_reset_tokens`, `roles`, `client_roles`, `services`, `masters`, `master_services`, `master_schedule`, `master_time_blocks`, `booking_holds`, `booking_hold_services`, `appointments`, `appointment_services`, `notifications`. У мастера-аккаунта — `masters.client_id`.
- Видимость записей — объединение ролей из БД: клиент — свои, мастер — своё расписание, администратор — все. Отмена и перенос — только владелец. Регистрация и новый OAuth-аккаунт — всегда роль `client`. Login/register/yandex режутся по частоте (`req.ip`; `TRUST_PROXY` только за своим nginx).
- Свободное время **не хранится**. Нет `slots` / `time_slots` / `free_slots`. Расчёт в момент запроса: график мастера − записи − `master_time_blocks` − живые холды; плюс `studio_closures` и `min_lead_minutes`. Занятость записи: статусы `pending_prepayment`, `confirmed`, `rescheduled` (не `cancelled` / `expired`). Интервалы полуоткрытые `[start, end)`.
- Резерв до «Подтвердить запись» — `booking_holds` + `booking_hold_services`, не статус записи. После входа и подтверждения появляется `appointments` со статусом `pending_prepayment`. Черновик гостя: `hold_token` можно дублировать в `sessionStorage`, состав услуг — в БД.
- Статусы записи (код в колонке): `pending_prepayment` / `confirmed` / `cancelled` / `rescheduled` / `expired`. Русская подпись — в UI. Шестого статуса «посещена» нет: прошедшее = время + текущий статус.
- Даты: инстанты — ISO 8601 UTC с суффиксом `Z` (`2026-08-26T07:00:00Z`); календарный день студии — `YYYY-MM-DD`; часы графика — `HH:MM`; пояс — IANA `Europe/Moscow`.
- Факт в одной таблице, в других только внешний ключ (JOIN): не копировать имя мастера, адрес студии и текст уведомления в запись. **Исключение:** цена и длительность уже оформленного визита — снимок в `appointment_services` (`price_rub`, `duration_min_minutes`, `duration_max_minutes`) на момент `insertAppointment`; актуальный прайс остаётся в `services`. Название услуги по-прежнему JOIN. Услугу с визитами не стирать: `DELETE` без ссылок удаляет строку, иначе `services.is_active = 0` (клиенту в `GET /api/services` и степпере не показывать).
- FAQ, политика, правила, баннер сертификата — `content_pages` + `content_page_items` на бэкенде; клиентских GET этих страниц нет — копирайт с макета, без выдуманных штрафов. Точный адрес — `studio_settings.exact_address`, на экране только при статусе `confirmed`.
- Не храни платёжные реквизиты и колонку «предоплата получена». Факт предоплаты = статус `confirmed`. Клиентский UI статус показывает, платёж не проводит.

Пишущий SQL и ломающие схему команды — только если человек явно попросил. Перед запросом покажи его в чат, после — сколько строк затронуто. Новые таблицы слотов не добавляй, даже «для удобства».

## Вне scope v1

Оплата, кабинет мастера, CRM, чат, карты, выезд, тёмная тема, отзывы-карусель, сторис, аналитика как продукт. Админка услуг, мастеров и записей — в scope и уже собрана; не пересобирать.

Не имитировать: полное восстановление пароля (письма / `password_reset_tokens` / смена по ссылке), смену пароля в профиле. «Забыли пароль?» не превращать в reset-API: только check `password-login-available` для аккаунтов без пароля (Яндекс). Лента уведомлений уже есть (API + `cabinet-notifications.html`); не дублировать.

## Продолжение в новом чате

> Продолжи «Ноготочки». Прочитай `cloud.md`, `docs/frontend-rules.md`, `docs/ui-map.md`, `docs/database.md`, `docs/test-checklist.md`, `figma.md`, паспорт, `Путь клиента.md` и `docs/db-schema.md`. Репозиторий: https://github.com/khramov-nikita/nogotochki. Продуктовый UI — HTML/CSS/JS в `web/` (не React, не v0): лендинг `index.html`, вход/регистрация (в т.ч. «Войти через Яндекс»), степпер `booking*.html`, кабинет `cabinet*.html` (в т.ч. уведомления), админка `/admin` + `/admin/services` + `/admin/masters` (записи дня живые, не заглушка). Узкий экран — `web/responsive.css` + меню в `header.js`, десктоп не переписывать. Черновик — `sessionStorage` в `web/js/store.js`. Все HTTP — `web/js/api.js`. Vite на :5173 проксирует `/api` на API :3000; HTML `/admin*` — тоже на API, **не** `/admin.css`. Живая SQLite в `server/`: `node:sqlite`, Node ≥ 22.16, файл `data/nogotochki.sqlite` (не в git). Миграции `005`–`008` (снимок цены, админ-записи, уведомления, OAuth). HTTP API — `npm start` из `server/`. Прод файла БД — BeGet, не Vercel. Таблицы свободных слотов нет, клиент это `clients`, пароль — salted scrypt (может быть NULL). Внешний вход — `GET /api/auth/yandex/start` → callback → `POST /api/auth/yandex`; env `YANDEX_CLIENT_ID` / `YANDEX_CLIENT_SECRET` / `YANDEX_REDIRECT_URI`. Роли списком в `client_roles`. `server/` не трогай без просьбы. Не переписывай уже собранные запись, кабинет, адаптив, админку и вход через Яндекс. Ручная проверка 2026-09-10 — в `docs/test-checklist.md`; находки чинить только по просьбе и в порядке из чек-листа. Ещё нет отдельных HTML: каталоги, карточка мастера, политика, служебные empty, профиль. Reset-HTTP не подключать; «Забыли пароль?» — только check для Яндекс-only. Оплаты нет, адрес только после подтверждения, на шаге 3 — таймер hold, на переносе — нет. Дедлайн «за 24 часа» не писать.
