# Установленные зависимости — ставятся заново в образе
**/node_modules

# Локальная / тестовая база — иначе затрёт боевую на томе
data/
**/*.sqlite
**/*.sqlite-wal
**/*.sqlite-shm
**/*.sqlite-journal

# Секреты и локальные настройки
.env
.env.*
!.env.example

# История изменений
.git
.gitignore
