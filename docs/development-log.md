# Журнал разработки — итог ручной проверки (2026-09-10)

Находки групп 1–2 из `docs/test-checklist.md` исправлены и повторно проверены (включая связанные сценарии). Краткие детали также в журнале `docs/database.md`.

## Что было сломано, причина и исправление

### G4. Нет «Дизайн ногтей» на шаге 1 записи

- **Сломано:** на шаге выбора услуг не было добавки «Дизайн ногтей».
- **Причина:** в `seed-dev.js` не было `nail_design`; локальная БД после `db:dev` тоже без добавки.
- **Исправление:** в seed-dev добавлен `nail_design` (`is_addon = 1`); услуга дописана в локальную SQLite.

### C17. Нет empty «Нет свободного времени» на переносе

- **Сломано:** дни без окон на переносе нельзя было выбрать; empty не показывался.
- **Причина:** в `cabinet-reschedule.js` пустые дни получали `disabled`, клик игнорировался.
- **Исправление:** в горизонте пустые дни кликабельны (вид unavailable без `disabled`); при смене дня сбрасывается выбранный слот.

### S3. Неверный текст при конфликте слота на confirm

- **Сломано:** после чужого занятия слота на подтверждении показывалось «Резерв истек…» вместо «Это время уже занято».
- **Причина:** purge стирал холд до проверки занятости слота.
- **Исправление:** снимок холда до purge + `SLOT_TAKEN`, если слот занят (`holds.js`); fallback в `booking-review.js`, если холд уже стёрт.

### G3. Нет подарочного сертификата / «Условия в студии»

- **Сломано:** на лендинге не было карточки сертификата и кнопки «Условия в студии».
- **Причина:** в `seed-dev.js` не было `gift_certificate`.
- **Исправление:** в seed-dev добавлен `gift_certificate` (`is_bookable = 0`, цены 3000/5000, срок 6 мес.); услуга дописана в локальную SQLite. UI уже различал CTA по флагу.

## Косметические недочёты (остались)

Отдельных косметических находок по итогам проверки нет.

# Журнал разработки — домен (2026-09-15)

Купил домен для сервиса: `nogotochki-test.ru`, регистратор BeGet, регистрация оплачена до 15.09.2027. Идентификация администратора через Госуслуги пройдена. A-запись: `91.106.207.106`; сервер ещё не заказан — вернусь к этому после его установки.

Решение: сервис работает на вымышленных и моих собственных данных; чужие настоящие данные в него не заводим, реальных клиентов не приглашаем.

# Журнал разработки — подготовка к серверу (2026-09-15)

Проект подготовлен к запуску в контейнере: добавлены `Dockerfile` (Node 24.19.0) и `.dockerignore` (зависимости, локальная база, `.env`, `.git` в образ не копируются).

Файл базы живёт отдельно от кода, который при обновлении контейнера стирается. Путь берётся из настройки `DATABASE_PATH` (не зашит в логику). Локально по умолчанию — `data/nogotochki.sqlite` от корня репозитория; в Docker по умолчанию — `/data/nogotochki.sqlite` (том на сервере). Папка под файл создаётся сама при первом запуске, если её ещё нет.

Миграции применяются приложением при старте (`applyMigrations` до `listen`); уже записанные в `schema_migrations` повторно не выполняются. Вручную `npm run migrate` на сервере не нужен.

## Настройки на сервере

| Переменная | Задать на сервере? | По умолчанию |
|---|---|---|
| `DATABASE_PATH` | Да, если том не `/data` | в образе: `/data/nogotochki.sqlite`; локально без env: `data/nogotochki.sqlite` |
| `NODE_ENV` | Желательно `production` | в образе: `production`; иначе `development` |
| `PORT` | Только если порт не 3000 | `3000` |
| `CORS_ORIGIN` | Да, если фронт на другом origin и нужны cookie | пусто → отражать Origin запроса |
| `SESSION_DAYS` | Нет | `30` |
| `TRUST_PROXY` | Да, если перед Node стоит nginx/прокси | пусто/`0`; за nginx обычно `1` |
| `YANDEX_CLIENT_ID` | Да, для входа через Яндекс | пусто |
| `YANDEX_CLIENT_SECRET` | Да, для входа через Яндекс | пусто |
| `YANDEX_REDIRECT_URI` | Да, как в кабинете Яндекса | пусто |
| `DEV_*_PASSWORD` | Нет на сервере | только для локального `seed:dev` |
| `BOOTSTRAP_ADMIN_EMAIL` / `BOOTSTRAP_ADMIN_PASSWORD` | Да на пустой БД в Coolify | нет в git; создаёт первого администратора при старте |

Раньше здесь были `YANDEX_OAUTH_STUB` и `YANDEX_STUB_*`: временная заглушка входа через Яндекс, на сервере не включать, убрать при публикации. **Закрыто 2026-09-21** — заглушка удалена, работает настоящий OAuth (см. запись ниже).

# Журнал разработки — деплой на Coolify (2026-09-21)

Сервис поднят на Coolify с репозитория https://github.com/khramov-nikita/nogotochki (ветка `main`). Разворачивается по файлу описания запуска `docker-compose.yaml` (+ сборка из `Dockerfile`): порт `3000`, `NODE_ENV=production`, статика `web/`, `TRUST_PROXY=1`. Для входа через Яндекс на сервере задаются `YANDEX_CLIENT_ID`, `YANDEX_CLIENT_SECRET`, `YANDEX_REDIRECT_URI`.

Данные хранятся снаружи контейнера: Docker-том `nogotochki_data` → внутри `/data/nogotochki.sqlite` (`DATABASE_PATH`). Повторный деплой проверен — записи клиентов на месте, том не стирается.

Отдельной секретной строки для подписи сессий в проекте нет и в compose её нет: сессия — случайный токен в cookie, в БД хранится его SHA-256. Пароль первого администратора — только из env Coolify (`BOOTSTRAP_ADMIN_EMAIL` / `BOOTSTRAP_ADMIN_PASSWORD`), не из git.

Публичный адрес проверки: `http://159.194.227.249:3000`.

Compose переименован в `docker-compose.yaml` (Coolify ожидает это имя).

# Журнал разработки — сессия на HTTP-проде (2026-09-21)

### Регистрация / вход не держали кабинет на Coolify

- **Сломано:** после «Зарегистрироваться» страница снова открывала вход/регистрацию, кабинет не открывался.
- **Причина:** при `NODE_ENV=production` cookie сессии всегда ставилась с флагом `Secure`. Сайт на Coolify открыт по **HTTP**, браузер такую cookie не сохраняет → `GET /api/auth/me` даёт 401 → редирект на auth.
- **Исправление:** `shouldUseSecureCookies(req)` в `server/src/http/cookies.js` — флаг `Secure` только при реальном HTTPS (`req.secure` или `x-forwarded-proto: https`). Используется в login/register/yandex/logout. Тест: `server/src/http/cookies.test.js`.
- **Проверка:** после деплоя коммита `3020063` регистрация на `http://159.194.227.249:3000` открывает кабинет и сессия сохраняется после обновления страницы.

# Журнал разработки — настоящий вход через Яндекс (2026-09-21)

Временное решение для входа через Яндекс убрано. Вместо заглушки `YANDEX_OAUTH_STUB` работает настоящая интеграция: `GET /api/auth/yandex/start` → согласие в Яндексе → `/auth/yandex/callback` → обмен `code` на профиль в `fetchYandexUserProfile` → прежняя выдача своей сессии. Токен Яндекса не сохраняется. При отказе или ошибке Яндекса пользователь возвращается на экран входа с понятным сообщением.

Идентификатор и секрет приложения (`YANDEX_CLIENT_ID`, `YANDEX_CLIENT_SECRET`) и `YANDEX_REDIRECT_URI` задаются переменными окружения на сервере (Coolify). В репозитории и в git их нет — только пустые поля в `.env.example`. Файл `.env` в git не коммитится.

Кнопка входа на `auth.html` и `register.html` приведена к требованиям Яндекса к оформлению: тип main, тема light, размер L (56px), скругление 0; цвет, иконка, текст и внутренние отступы — по бренд-требованиям, не из `tokens.css`. OAuth-поток прежний (не SDK Suggest).

# Журнал разработки — сервис опубликован и принят (2026-09-21)

Сервис опубликован и принят. Открывается по адресу `https://nogotochki-test.ru`; дата публикации — 21.09.2026.

На этом адресе прогнан сокращённый чек-лист после публикации; результаты — в [`docs/prod-checklist.md`](prod-checklist.md) (протоколы по сценариям — в `docs/prod-check-results/`).

Копия файла базы (`nogotochki.sqlite` с тома сервера, `/data/nogotochki.sqlite`) скачана на компьютер 21.09.2026 около 22:55 (локально: `Desktop\nogotochki.sqlite`; на момент копирования: 4 клиента, 2 записи, 7 услуг).

Автодеплой Coolify проверен push-ом в основную ветку `main`: после пуша сервис пересобрался и поднялся сам.

По домену: остаёмся на `nogotochki-test.ru` (регистратор BeGet); регистрация оплачена до 15.09.2027. Решение прежнее — сервис на вымышленных и своих данных, чужие настоящие данные и реальных клиентов не заводим.

## За рамками (можно вернуться)

- Перенос базы на полноценную СУБД
- Уведомления по почте
- Бэкапы по расписанию
