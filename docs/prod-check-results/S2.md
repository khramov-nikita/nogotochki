# S2. Два человека на один слот при резерве `[2 окна]`

- **ID:** S2
- **Статус:** pass
- **Дата:** 2026-09-21T19:34:52Z
- **База:** https://nogotochki-test.ru
- **Что сделали:**
  1. Два независимых Playwright-контекста (гость A + гость B) дошли до шага 3 (`booking-datetime.html`), мастер id=1, услуга id=1.
  2. A выбрал день **23 сентября 2026** и слот **11:30** (`starts_at` 2026-09-23T08:30:00Z) → `POST /api/holds` **201**, таймер «Слот зарезервирован · 14:59».
  3. B на уже открытом списке того же дня кликнул то же время **11:30** → `POST /api/holds` **409** `SLOT_TAKEN`.
  4. Повторная проверка API без токена A: `POST /api/holds` на тот же `starts_at` → **409** `SLOT_TAKEN`.
  5. Холд A снят `DELETE /api/holds/:token` → **200**. Browser MCP не использовался — два окна через Playwright.
- **Ожидали:** у B «Это время уже занято»; запись второму не создаётся.
- **Увидели:**
  - A: hold 201, `#hold-timer` виден («Слот зарезервирован · 14:59»).
  - B UI: `#slot-error` = «Это время уже занято»; `error.code=SLOT_TAKEN`; таймер B не появился; `booking-done` не открылся.
  - B API: 409, message «Это время уже занято», подсказка `slots` (5).
- **Заметки:** Гонка холдов на HTTPS отрабатывает как локально: второй hold отклоняется на шаге 3 до confirm. Appointment для B не создавался (гость, hold failed). Evidence: `docs/prod-check-results/_s2-evidence.json`.
