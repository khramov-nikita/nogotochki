# S4. Выход закрывает кабинет и админку

- **ID:** S4
- **Статус:** pass (кабинет); admin blocked
- **Дата:** 2026-09-21T20:34:00Z
- **База:** https://nogotochki-test.ru
- **Что сделали:** Playwright Chromium headless. Клиент: вход `c5probe_1790018461@nogotochki.test` / `DevClient123!` → `cabinet.html` → «Выйти» в шапке → повторный заход на `cabinet.html`. Админ: `POST /api/auth/login` seed `admin@nogotochki.test` / `DevAdmin123!` (UI-выход из `/admin` не выполнялся — нет сессии).
- **Ожидали:** После выхода — лендинг / шапка с «Войти»; повторный кабинет и админка требуют нового входа (редирект на auth).
- **Увидели:**
  - Клиент: `POST /api/auth/logout` → 200; редирект на `index.html`; в шапке «Войти», «Выйти» нет; cookie `session` сброшена (`Secure`+`HttpOnly` до выхода); `GET /api/auth/me` → 401 «Нужна авторизация»; повторный `cabinet.html` → `auth.html?next=cabinet.html`, форма «Вход», чужих записей нет.
  - Админ: login seed → **401** `UNAUTHORIZED` «Неверный email или пароль». Повторный `/admin` после админского logout не проверялся.
- **Заметки:** Seed admin на проде отсутствует (тот же блокер, что A3/A14/A15). Клиентская половина S4 на HTTPS с Secure-cookie подтверждена. Скрипт: `docs/prod-check-results/_s4-run.mjs`, сырьё: `_s4-evidence.json`.
