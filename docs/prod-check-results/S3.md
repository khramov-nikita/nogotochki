# S3. Подтверждение после того, как слот заняли иначе `[2 окна]`

- **ID:** S3
- **Статус:** pass
- **Дата:** 2026-09-21T19:34:21Z
- **База:** https://nogotochki-test.ru (HTTP :3000 не понадобился)
- **Что сделали:** Playwright Chromium, два изолированных context (A/B). Зарегистрированы `prodcheck-s3a-1790019261984@example.com` и `prodcheck-s3b-…`. A: шаги 1–4, холд на 2026-09-22 16:30 MSK (`starts_at` `2026-09-22T13:30:00Z`, `POST /api/holds` 201). Чтобы B мог занять то же окно без ожидания полных `reserve_minutes` (15), A снял свой холд (`DELETE /api/holds/:token` 200) — имитация истечения/purge, при этом A остался на `booking-review.html` с токеном в памяти страницы. B: тот же слот 16:30 → confirm → `booking-done.html` («Запись создана»). A: «Подтвердить запись».
- **Ожидали:** у A «Это время уже занято»; успешного «Готово» на чужой слот нет.
- **Увидели:** `POST /api/appointments` у A → 409 `HOLD_EXPIRED` («Резерв истек или не найден»); UI на `booking-review.html` показал empty/баннер **«Это время уже занято»** (fallback S3), альтернатива «Другое время»; текста «Резерв истек…» как единственного ответа нет; `booking-done` у A не открылся; записи у A нет. У B — «Готово» на тот же слот.
- **Заметки:** Гонка на HTTPS подтверждена двумя сессиями. Освобождение холда A через DELETE — контролируемый proxy истечения (иначе ждать ~15 мин `reserve_minutes`). Запись B на 22.09 16:30 могла остаться (отмена после прогона не выполнена). Evidence: `docs/prod-check-results/_s3-evidence.json`, runner `_s3-run.mjs`.
