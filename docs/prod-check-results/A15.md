# A15. Пустой день в списке записей

- **ID:** A15
- **Статус:** blocked
- **Дата:** 2026-09-21T19:30:00Z
- **База:** https://nogotochki-test.ru (fallback http://159.194.227.249:3000 — тот же результат)
- **Что сделали:** Попытка входа seed-админом `admin@nogotochki.test` / `DevAdmin123!` через `POST /api/auth/login` (только HTTP-код, без cookie). Без сессии: `GET /api/admin/appointments?date=2026-10-04`. Список дня в `/admin` не открывался.
- **Ожидали:** Дата без визитов в `/admin`: empty списка; нет клиентского CTA «Записаться» как у пустого кабинета.
- **Увидели:** HTTPS и HTTP login → **401**; `GET /api/admin/appointments?date=…` без cookie → **401**. UI empty-дня и отсутствие CTA «Записаться» не проверялись — нет админской сессии.
- **Заметки:** Совпадает с известным prod-фактом и C11: seed admin на сервере отсутствует. Для разблокировки нужны `BOOTSTRAP_ADMIN_*` / seed admin в БД.
