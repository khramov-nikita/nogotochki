# A14. Блокировка времени мастера [2 окна]

- **ID:** A14
- **Статус:** blocked
- **Дата:** 2026-09-21T19:30:00Z
- **База:** https://nogotochki-test.ru (HTTP :3000 тоже 200; оба проверены)
- **Что сделали:** Проверили доступность баз (HTTPS и HTTP → 200). Попытка админского входа seed `admin@nogotochki.test` / `DevAdmin123!`: `POST /api/auth/login` на обоих базах. Без админ-сессии создать `POST /api/admin/time-blocks` нельзя — сценарий `[2 окна]` (админ блокирует → гость на шаге 3 не видит старты) остановлен. Дополнительно как гость: `GET /api/masters/1/availability` — есть свободные окна, которые можно было бы резать блоком.
- **Ожидали:** Админ блокирует часы мастера; у гостя/клиента на шаге 3 эти старты больше не в свободных слотах (границы по серверному времени / TZ).
- **Увидели:** `POST /api/auth/login` admin → **401** `UNAUTHORIZED` «Неверный email или пароль» на `https://nogotochki-test.ru` и на `http://159.194.227.249:3000`. Блокировку создать не удалось. Гостевая availability (мастер 1, service 1): `2026-09-22` — 1 слот (`16:30`); `2026-09-23` — 11 слотов (`11:30`…); `2026-09-24` — 9 слотов (`10:00`…). Эффект блока на слоты **не проверялся**.
- **Заметки:** Seed admin на проде отсутствует (тот же блокер, что у C11 admin confirm). Нужен `seed:dev` / ручной сид администратора или выданная учётка с ролью `administrator`, затем повтор: break на известное окно → сверка availability/UI шага 3 → `DELETE /api/admin/time-blocks/:id`. Обратимый cleanup не выполнялся — блок не создавался.
