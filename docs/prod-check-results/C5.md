# C5. Вход тестового клиента

- **ID:** C5
- **Статус:** pass
- **Дата:** 2026-09-21T19:21:00Z
- **База:** https://nogotochki-test.ru
- **Что сделали:** Browser MCP tab не удерживался (create → navigate «view not found») — проверка через Playwright Chromium headless. 1) Вход `client@nogotochki.test` / `DevClient123!` на `/auth.html`. 2) Seed-учётки нет → регистрация `c5probe_1790018461@nogotochki.test` / `DevClient123!` → кабинет → выход → повторный вход той же учёткой.
- **Ожидали:** Успешный вход существующей учёткой → кабинет (список активных или empty); без «Неверный email или пароль».
- **Увидели:** Seed: `POST /api/auth/login` → 401 `UNAUTHORIZED` «Неверный email или пароль», остаёмся на `auth.html`. После регистрации и выхода: `POST /api/auth/login` → 200; редирект на `cabinet.html`; title «Кабинет — Ноготочки»; h1 «Кабинет»; empty «В кабинете пока нет записей» + «Записаться»; текста «Неверный email или пароль» нет; cookie `session` Secure+HttpOnly; `GET /api/auth/me` → client id 7; `GET /api/appointments?scope=active` → `[]`.
- **Заметки:** Сид `client@nogotochki.test` на проде отсутствует (нужен `seed:dev` или ручной сид). Сценарий C5 по login + серверной БД подтверждён на свежесозданной учётке.
