# C11. Полная запись до «Готово»

- **ID:** C11
- **Статус:** pass (подшаг admin confirm — blocked)
- **Дата:** 2026-09-21T19:22:34Z
- **База:** https://nogotochki-test.ru (HTTP :3000 не понадобился; оба отдают 200)
- **Что сделали:** Browser MCP tab не удерживался (create → navigate «view not found» / «No browser tab available») — проверка через Playwright Chromium headless. Seed `client@nogotochki.test` / `admin@nogotochki.test` на проде дают 401. Зарегистрировали клиента `prodcheck-c11-1790018538218@example.com`, cookie `session` (Secure, HttpOnly, SameSite=Lax). Шаги 1–4: услуга «Маникюр с покрытием гель-лаком», мастер «Мастер студии», слот 2026-09-23 10:00 (`POST /api/holds` 201, `expires_at` серверный), «Подтвердить запись» → `POST /api/appointments` 201. Админский вход для подтверждения предоплаты — 401 (seed отсутствует).
- **Ожидали:** Шаги 1–4 → «Готово»; статус «ожидает предоплату»; предоплату принимает студия; точного адреса нет; «В кабинет»; при возможности студия подтверждает предоплату.
- **Увидели:** `booking-done.html`, h1 «Запись создана»; intro «Статус «ожидает предоплату». Предоплату принимает студия вне сайта…»; badge «Ожидает предоплату»; в сводке услуга/мастер/23 сентября 2026, 10:00; строк «Адрес студии» / уличного адреса нет; `exact_address: null`; CTA «В кабинет» → `cabinet.html`. Админ confirm не выполнен: `POST /api/auth/login` admin → 401 «Неверный email или пароль».
- **Заметки:** Сквозной путь на HTTPS (TZ слот 10:00 MSK = `07:00Z`, hold cookie-сессия, confirm) — pass. Подшаг «студия принимает предоплату» в UI/API — **blocked** из‑за отсутствующего admin seed на сервере. Одна реальная запись на 23.09 10:00 оставлена (допустимо для prod-check).
