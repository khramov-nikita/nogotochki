# C1. Регистрация нового клиента

- **ID:** C1
- **Статус:** pass
- **Дата:** 2026-09-21T19:18:00Z
- **База:** https://nogotochki-test.ru (fallback HTTP :3000 не понадобился; оба отдают 200)
- **Что сделали:** Browser MCP tab не удерживался (create → navigate «view not found» / «No browser tab available») — проверка через Playwright Chromium headless. С лендинга «Войти» → `auth.html` → «Зарегистрироваться» → `register.html`. Новая почта `prodcheck-c1-1790018326@example.com`, пароль ≥8 с совпадающим повтором. Отправка формы.
- **Ожидали:** После успеха — кабинет; при отсутствии записей — «В кабинете пока нет записей» + «Записаться»; на регистрации в шапке нет повторной «Войти»; сессия (Set-Cookie / Secure) держит кабинет.
- **Увидели:** `POST /api/auth/register` → 201; cookie `session` (`Secure`, `HttpOnly`, `SameSite=Lax`, domain `nogotochki-test.ru`); редирект на `cabinet.html` (title «Кабинет — Ноготочки»); empty «В кабинете пока нет записей», кнопка «Записаться»; `GET /api/auth/me` → 200 (client id 5); `GET /api/appointments?scope=active` → `{"appointments":[]}`. На `register.html` в шапке ссылки «Войти» не было.
- **Заметки:** Secure-cookie на HTTPS после регистрации подтверждена. Playwright видит cookie в контексте браузера; в `response.headers()['set-cookie']` значение не отдалось (ограничение API ответа), но сессия и `/api/auth/me` 200 после редиректа подтверждают установку.
