# G13. Кабинет без входа

- **ID:** G13
- **Статус:** pass
- **Дата:** 2026-09-21T19:03:00Z
- **База:** http://159.194.227.249:3000 (HTTPS на :3000 не отвечает; https://159.194.227.249/ → 503)
- **Что сделали:** Чистый контекст без cookies. Прямой заход на `/cabinet.html`. Browser MCP tab не удерживался (create → navigate «view not found») — проверка через Playwright Chromium headless против той же базы. Параллельно: `GET /api/auth/me` и `GET /api/appointments` без cookie → 401.
- **Ожидали:** Редирект на вход с `next=` на кабинет; формы кабинета с чужими записями нет.
- **Увидели:** Финальный URL `http://159.194.227.249:3000/auth.html?next=cabinet.html`; title «Вход — Ноготочки»; h1 «Вход»; поля почта/пароль; `.appointment-card` = 0; cookies = 0. На пути: `GET /api/auth/me` → 401 `UNAUTHORIZED` («Нужна авторизация»), затем клиентский `location.replace` из `require-client.js`.
- **Заметки:** Secure-cookie на HTTPS в этом прогоне не проверялся — жив только HTTP :3000. На HTTP сценарий редиректа/`next=` и отсутствия чужих записей отрабатывает как ожидается.
