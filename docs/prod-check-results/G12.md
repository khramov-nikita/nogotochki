# G12. Истёкший резерв на проверке

- **ID:** G12
- **Статус:** pass
- **Дата:** 2026-09-21
- **База:** http://159.194.227.249:3000 (HTTPS на :3000 недоступен)
- **Что сделали:**
  1. TTL резерва: `studio_settings.reserve_minutes = 15` (seed), холд получает серверный `expires_at`.
  2. `POST /api/holds` на свободный слот master=1 / service=1 / `2026-09-23T13:30:00Z` → `hold_token=f41ec21a…`, `expires_at=2026-09-21T19:16:00Z` (~15 мин от создания ~19:01Z).
  3. Playwright: draft в `sessionStorage` → `booking-review.html` (шаг 4, гость) до истечения; дождались `expires_at`; затем повторный заход на review с тем же токеном.
  4. `GET /api/holds/:token` после истечения.
- **Ожидали:** после конца резерва на шаге 4 — текст про истёкший резерв; «Подтвердить» неактивна или путь начать запись / выбрать время снова. Истечение по серверному `expires_at`.
- **Увидели:**
  - До истечения (~19:08Z): шаг 4 «Проверка записи»; таймер «Слот зарезервирован · 07:31»; «Подтвердить запись» `disabled`; ошибки нет.
  - После `expires_at` на той же странице (~19:16:10Z): текст «Резерв истек или не найден»; таймер скрыт; «Подтвердить запись» остаётся `disabled`.
  - API: `409` `HOLD_EXPIRED` / «Резерв истек или не найден».
  - Повторный заход на review с истёкшим токеном: редирект на `booking-datetime.html` (выбор времени заново).
- **Заметки:** Browser MCP tabs не открывались (`No browser tab available`); проверка UI через Playwright. HTTPS не отвечал. Гость: Confirm и до истечения неактивна — критерий закрыт текстом об истечении + disabled Confirm и/или редиректом на шаг 3.
