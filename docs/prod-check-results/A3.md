# A3. Вход администратора

- **ID:** A3
- **Статус:** blocked
- **Дата:** 2026-09-21T19:28:38Z
- **База:** https://nogotochki-test.ru
- **Что сделали:** `POST /api/auth/login` учёткой из README / `seed:dev`: `admin@nogotochki.test` / `DevAdmin123!`. В docs/README других известных паролей админа нет (`BOOTSTRAP_ADMIN_*` только в Coolify env, не в git — не подбирали). Без сессии проверили `GET /admin`.
- **Ожидали:** Успешный вход админом → админка (список дня / разделы); Secure-cookie админской сессии на HTTPS.
- **Увидели:** Login → **401** `UNAUTHORIZED` «Неверный email или пароль»; Set-Cookie сессии нет. `GET /admin` без сессии → **302** `Location: /auth.html?next=%2Fadmin`. UI `/admin` (список дня / разделы) не открывался.
- **Заметки:** Seed-админ на проде отсутствует (как у C5/C11). Чтобы разблокировать A3/A14/A15: `seed:dev` на серверной БД **или** известные `BOOTSTRAP_ADMIN_EMAIL` / `BOOTSTRAP_ADMIN_PASSWORD` из панели Coolify. Учётные данные не изобретались.
